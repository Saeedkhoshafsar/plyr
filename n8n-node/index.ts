// n8n discovers nodes/credentials via the "n8n" field in package.json, so this
// entry point is intentionally empty. It exists only so `main` resolves and the
// package can be required without error.
export {};
